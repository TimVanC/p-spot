// Client-side EXIF extraction using exifr.
// GPS data is required; if absent the submission is rejected.

export async function extractExif(_imageUri: string): Promise<never> {
  throw new Error('extractExif: not yet implemented');
}
